import { NgZone } from '@angular/core';
import { Subject } from 'rxjs/Subject';
export interface InViewportServiceRegistryObject {
    targets: Element[];
    rootElement: Element;
    observer: IntersectionObserver;
}
export declare class InViewportService {
    private ngZone;
    private registry;
    readonly trigger$: Subject<IntersectionObserverEntry>;
    constructor(ngZone: NgZone);
    addTarget(target: Element, rootElement?: Element): void;
    removeTarget(target: Element, rootElement?: Element): void;
    private getRootElement(element);
    private findRegistryEntry(rootElement);
    private onChanges(entries);
    private register(target, rootElement?);
    private unregister(target, rootElement?);
}
